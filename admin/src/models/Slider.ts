export interface ISlider {
    id: string;
    position: number;
    imageName?: string;
    imageSrc?: string;
    redirectTo: string;
    isVisible: boolean;
    redirectToId: string;
    imageFile:any;
}